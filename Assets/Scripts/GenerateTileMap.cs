using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateTileMap : MonoBehaviour
{
    [SerializeField]
    GameObject[] tile;

    private void Awake()
    {
            
    }

    //TODO: Make a scriptable asset class that contains all of the information about how to set up the tiles for the initial generation of each level
    private void Start()
    {
        
    }

    // Update is called once per frame
    private void Update()
    {
        
    }
}
